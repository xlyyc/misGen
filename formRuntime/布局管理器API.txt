    //获得默认的单元格高度
    getItemHeight

//设置默认的单元格高度
    setItemHeight

    //获得默认的列数
    getCols

     //设置默认的列数
    setCols

    //获得当前激活的section index
    getActiveSectionIndex

    //设置当前激活的sectionIndex
    setActiveSectionIndex

    //获得当前激活的item行列号
    getActiveItemRank

    /**
    *设置当前激活的item行列号
    * 返回形如 {row:1,col:1}
    */
    setActiveItemRank


    /**
     * 插入新的section
     * sectionData section数据
     * sectionIndex 在指定section序号前插入(序号从1开始)
     * 如果sectionIndex为null 缺省在最后插入
     */
    insertSection (sectionData, sectionIndex)

    /**
     * 在指定item插入节点
     * 如果itemRank和sectionIndex为null 则在当前焦点的item中插入
     * node 节点对象
     * itemIndex 在指定item序号内插入(序号从1开始)
     * sectionIndex section 序号
     */
    insertNode (node, itemRank, sectionIndex)

    /**
     * 获得section的个数
     */
    getSections

    /**
     * 获得指定sectionIndex的section包含有item的个数
     */
    getItems (sectionIndex)

    /**
     * 上移指定序号的section
     * sectionIndex 指定的section序号(序号从1开始)
     */
    upSection (sectionIndex)

    /**
     * 下移指定序号的section
     * sectionIndex 指定的section序号(序号从1开始)
     */
    downSection (sectionIndex)

    /**
     * 删除指定序号的section
     * sectionIndex 指定的section序号(序号从1开始)
     */
    deleteSection (sectionIndex)

    /**
     * 修改flowlayout
     * flowLayoutData flowLayout数据
     */
    updateFlowLayout (flowLayoutData)

    /**
     * 修改指定序号的section
     * sectionData section数据
     */
    updateSection (sectionData)

    /**
     * 修改指定序号的item
     * itemData item数据
     * itemData形如 {row:1,col:1, }
     */
    updateItem (itemData)

    /**
     * 删除指定序号的item
     * itemRank 指定的item行列号
     * sectionIndex 指定的section序号(序号从1开始)
     */
    deleteItem (itemRank, sectionIndex)

    //找到指定序号的sectoin
    findSectionByIndex (sectionIndex)

    //根据itemRank和sectionIndex定位到item并减少列数
    reduceItemColspan:function(itemRank, sectionIndex)

    //根据itemRank和sectionIndex定位到item并解锁
    unfixItem:function(itemRank, sectionIndex)

    //根据itemRank和sectionIndex定位到item并锁定
    fixItem (itemRank, sectionIndex)

    //根据itemRank和sectionIndex定位到item并增加列数
    addItemColspan (itemRank, sectionIndex)

    //根据itemRank和sectionIndex定位到item并减少行数
    reduceItemRowspan (itemRank, sectionIndex)

    //根据itemRank和sectionIndex定位到item并增加行数
    addItemRowspan (itemRank, sectionIndex)

    //根据itemRank和sectionIndex找到对应item中的对象
    getNode (itemRank, sectionIndex)
